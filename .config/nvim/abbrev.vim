" common
iab teh the

" js
iab ireact import React from 'react';<CR>
iab isytled import styled from 'styled-components';<CR>
iab iproptypes import ProptTypes from 'prop-types';<CR>

iab ret{ return {<CR>};<ESC>O
iab ret( return (<CR>);<ESC>O

iab fn{ const = () => {<CR><CR>};<ESC>kk0ea
iab fn( const = () => (<CR><CR>);<ESC>kk0ea

iab arrmap( .map((entry) => (<CR><CR>));<ESC>3k0ftciw
iab arrmap{ .map((entry) => {<CR><CR>});<ESC>3k0ftciw







